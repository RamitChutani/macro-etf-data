# Session Worklog - 2026-03-06

## Scope
- Resume development and expand ticker mapping with MSCI country ETFs.
- Harden inclusion filtering so broken/dead tickers do not reach dashboard outputs.
- Add dependent ticker dropdown behavior and dynamic ticker metadata display in dashboard.
- Add ETF-vs-FX decomposition (quote currency vs USD, USD-return view, WEO-implied country FX).
- Update dashboard layout and docs to reflect current behavior.

## Decisions and Clarifications
- Keep script-first architecture; no structural refactor.
- Use automatic inclusion filtering in pipeline instead of manual keep/remove list.
- Expand allowed exchanges from LSE-only to LSE + NYSE/Nasdaq Yahoo exchange families.
- Keep `currency_hedged` as heuristic metadata flag (name-marker based) and mark uncertainty.
- For final dashboard annual panel, show FX decomposition fields only when selected ticker currency is not USD.

## Code Changes
- `src/etf_mapping.py`
  - Added additional MSCI country ETFs and added mapping entries for countries present in ISO map but previously missing ETF mappings.
- `src/fetch_etf_prices.py`
  - Added inspect-time and inclusion-time history quality gates:
    - min history rows (default `252`)
    - max staleness days (default `45`)
    - min acceptable history start (default `2020-01-01`)
  - Added candidate preflight options:
    - `--candidate-tickers`
    - `--inspect-output`
  - Added metadata enrichment:
    - `long_name`, `short_name`
    - `currency_hedged`, `currency_hedged_basis`
    - `history_start_date`, `history_end_date`, `history_rows`, `history_stale_days`
  - Expanded exchange allowlist to:
    - `LSE`, `PCX`, `NYQ`, `ASE`, `NMS`, `NGM`, `NCM`, `NAS`
- `src/build_combined_etf_weo.py`
  - Added WEO local-currency GDP level handling (`NGDP` -> `gdp_current_lcu`).
  - Added WEO-implied country FX series:
    - `country_lcu_per_usd_weo`
    - `country_lcu_vs_usd_weo_pct`
  - Added annual FX decomposition fields:
    - `etf_return_quote_pct`
    - `quote_ccy_vs_usd_pct`
    - `etf_return_usd_pct`
    - `etf_usd_minus_country_fx_pct`
  - Added `currency_hedged` propagation from metadata.
- `src/build_excel_dashboard_mvp.py`
  - Added country-specific ticker dropdowns in screener and focus panel.
  - Added dynamic ticker attribute lookup table on `Lists` and wired formulas:
    - screener `ticker_exchange` and `ticker_currency` now update from selected ticker.
    - focus controls now include dynamic `Ticker Exchange`.
  - Converted exchange codes to human-readable labels.
  - Moved country focus dashboard below screener (row-separated layout).
  - Added annual FX decomposition display columns and gated them for non-USD ticker selections only.
  - Preserved existing CAGR/timeframe logic while re-anchoring formulas to new layout.
- `README.md`
  - Updated capabilities and dashboard behavior to match implemented state.

## Validation and Runs
- Ran ticker inspect and full pipeline multiple times after edits:
  - `uv run python src/fetch_etf_prices.py --inspect-only`
  - `uv run python src/main.py`
  - `uv run python src/build_excel_dashboard_mvp.py ...`
- Confirmed generated artifacts:
  - `data/outputs/etf_prices.csv`
  - `data/outputs/etf_ticker_metadata.csv`
  - `data/outputs/weo_gdp.csv`
  - `data/outputs/etf_weo_combined_annual.csv`
  - `data/outputs/etf_gdp_dashboard_mvp.xlsx`
  - `data/outputs/etf_price_history_charts.xlsx`
- Verified in workbook/formulas:
  - dependent ticker dropdowns active
  - screener ticker exchange/currency formula-driven
  - country focus panel located below screener
  - annual FX columns rendered conditionally for non-USD selections

## Notes / Caveats
- `currency_hedged` currently resolves mostly to `unknown` due limited/ambiguous Yahoo naming metadata.
- WEO-based country FX is annual macro-implied (`NGDP/NGDPD`) and not tradable spot FX.
- Generated outputs were intentionally refreshed as part of validation.

---

## Session 2 (Continuity & Alignment)
- Aligned project agent with `BASE_AGENTS.md` and `AGENTS.md`.
- Updated `AGENTS.md` to reflect current March 6 status, including metadata enrichment, quality gates, and new dashboard capabilities (v0.6 in evolution log).
- Incorporated v0.7 refinements identified from commit history:
  - Hardened inception-year gating for disconnect/CAGR calculations.
  - Unified USD-based returns for all tickers to ensure valid macro comparisons.
  - Reformatted dashboard layout: sorted by economy size, added region grouping, and removed pane freezes.
  - Expanded ticker universe (`INDA`, `CSUS.L`) and transitioned to exchange blacklist.
  - Prioritized longest data availability for default ticker selection.
- Verified `src/main.py` orchestration logic correctly includes all recent pipeline additions.

---

## Session 3 (Dashboard Refinement)
- Updated `src/build_excel_dashboard_mvp.py` to refine the 'CAGR Comparison (%)' table:
  - Removed 'Real GDP - ETF CAGR %' and 'Nom USD GDP - ETF CAGR %' columns.
  - Reordered columns to align with the 'Annual Performance (%)' table: Horizon, ETF CAGR % (USD), Nominal GDP CAGR % (USD), Macro Disconnect %, Real GDP CAGR %, Nominal GDP CAGR % (LCU), ETF CAGR % (Local), and FX CAGR % (vs USD).
  - Updated conditional formatting for all returns on the `Country_CAGR_Summary` sheet (Screener, Timeframes, Annual, and CAGR tables) with a three-tier green saturation scale:
    - 0-5%: Light green (#E2F0D9)
    - 5-10%: Medium green (#C6E0B4)
    - 10%+: Saturated green (#A9D08E)
    - Negative: Light orange (#FCE4D6)
- Regenerated dashboard artifact: `data/outputs/etf_gdp_dashboard_mvp.xlsx`.

---

## Session 4 (Selection Policy & Layout Overhaul)
- Updated ticker selection criteria (Hard Gates):
  - Inception date must be on or before **2016-01-01**.
  - Must be **Accumulating** (cannot be distributing). Implemented proxy check via dividend history (last 3 years) and yield.
  - Cleared exchange blacklist (now empty).
- Refined dashboard layout and defaulting logic:
  - Default ticker selection priority: Currency (USD preference) -> Fund Size (AUM) -> Alphabetical.
  - Flipped GDP and ETF columns in Annual and CAGR focus tables to match the Screener (GDP first, ETF second).
  - Added a second gap column to the CAGR table for perfect vertical alignment with the Annual table.
  - Hardened Annual Macro Disconnect formula to return `NA()` if ETF return is 0% (indicating no history for that year).
- Regenerated all artifacts via end-to-end pipeline refresh.
